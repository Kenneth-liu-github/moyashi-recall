name: Swift Validation

on:
  workflow_dispatch:
  push:
    branches:
      - feature/v0.10-pre1-release-hardening

concurrency:
  group: swift-validation-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  core-tests:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Swift version
        run: swift --version
      - name: Run portable core tests
        run: swift test
      - name: Audit release project
        run: bash scripts/audit-release-project.sh

  ios-simulator-build:
    runs-on: macos-15
    steps:
      - uses: actions/checkout@v4
      - name: Run full macOS and iOS validation
        run: bash scripts/validate-v0.4-macos.sh
